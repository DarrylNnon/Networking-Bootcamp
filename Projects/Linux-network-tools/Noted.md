```sh
cat /etc/hosts
```

```sh
ping localhost
```

```sh
sudo nano /etc/hosts
```

```sh
ping www.google.com
```
```sh
cat /etc/systemd/resolved.conf
```

```sh
sudo nano /etc/systemd/resolved.conf
```

```sh
cat etc/nsswitch.conf
```
```sh
dig # we use it like nslookup
```
```sh
netstat
```

```sh
curl -I https://google.com
curl -X POST https://google.com
```

```sh
lsof -i # list all files
```

```sh
python -m http.server 8000 # to start a basic server in the background to test
```

```sh
ip addr
```

```sh
sudo tcpdump -i eth1
```

```sh
sudo dns search wireshark
```